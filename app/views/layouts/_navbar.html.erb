<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href=<%= root_path%> >Game Contest Server</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav">
                <li <% if current_page?(root_path) or current_page?(users_path) %> class="active" <% end %> >
                    <%= link_to 'View Users', users_path %>
                </li>

                <li <% if current_page?(contests_path) %> class="active" <% end %> >
                    <%= link_to "View Contests", contests_path %>
                </li>

                <li <% if current_page?(referees_path) %> class="active" <% end %> >
                    <%= link_to "View Referees", referees_path %>
                </li>


            </ul>

            <ul class="nav navbar-nav navbar-right">
                <% if ! logged_in? then %>
                    <!--
                    <li><%#= button_to "Log In", login_path, method: :get, class: 'btn btn-default navbar-btn' %></li>
                    <li><%#= button_to "Sign Up", signup_path, method: :get, class: 'btn btn-primary navbar-btn' %></li>
                    -->
                    <li><a href=<%= login_path %>>Log In</a></li>
                    <li><a href=<%= signup_path %>>Sign Up</a></li>
                <% else %>
                    <!-- TODO: Change
                    <li><a href=<%# new_player_path %>>New Player</a></li>
                    -->
                    <li <% if current_page?(new_contest_path) %> class="active" <% end %> >
                        <%= link_to "New Contest", new_contest_path %>
                    </li>
                    <li <% if current_page?(new_referee_path) %> class="active" <% end %> >
                        <%= link_to "New Referee", new_referee_path %>
                    </li>
                    <li><%= link_to "Log Out", logout_path, method: :delete %></li>
                    <!--
                    <li><%#= button_to "Log Out", logout_path, :method => :delete, class: 'btn btn-default navbar-btn' %></li>
                    -->
                <% end %>
            </ul>

            <!--
            <div class="nav navbar-nav navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="#">
                        <%= link_to "<button type='button' class='btn btn-default navbar-btn' >Sign up</button>".html_safe, signup_path %>
                        <% if ! logged_in? then %>
                            <li><%= link_to "Log In", login_path, :method => :get %></li>
                        <% else %>
                            <li><%= button_to "Log Out", logout_path, :method => :delete, class: 'btn btn-default navbar-btn' %></li>
                            <%= link_to 'Log Out', logout_path, method: :delete %>

                        <% end %>
                        <li><%= button_to "Sign up", signup_path, :method => :get, class: 'btn btn-default navbar-btn' %></li>
                    </ul>
                </div>
                -->

            </div><!-- /.navbar-collapse -->
        </div>
    </div>
</nav>
